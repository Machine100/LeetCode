/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
    
    var value1, value2
    for (value1=0; value1<nums.length; value1++) {
        for (value2=0; value2<nums.length; value2++) {
            var sum = nums[value1] + nums[value2]
            if (sum = target)
                break twice
                or keep in other values and continue algorithm.
        }
    }
    return (value1,value2)
};